#ifndef ALLPIECES_H
#define ALLPIECES_H

#include "BasePiece.hpp"
#include "types.hpp"
#include "debug.hpp"

BasePiece * makePiece(char color, char type, coord x, coord y)
{
    switch (type)
    {
    case 'P':
        /* code */
        break;
    
    default:
        
        break;
    }
}

#endif